# OWNERBOT Templates

**File:** `docs/OWNERBOT_TEMPLATES.md`  
**Role:** Каталог шаблонов OwnerBot (кнопки/быстрые сценарии + voice mapping).  
**Source-of-truth:** Выполнение любых действий через пайплайн **dry_run → confirm → commit** (см. `docs/OWNERBOT_ACTIONS_MASTER.md`).  
**Принцип:** Шаблоны закрывают 80% частых задач владельца. Голос используется для нестандартных/комбинированных запросов.

---

## 0) Общие правила шаблонов

### 0.1 Типы шаблонов
- **REPORT** (read-only): отчёты/снимки/диагностика. Без confirm.
- **ACTION** (write): любые изменения. Всегда preview + ✅ confirm.

### 0.2 UX-паттерн
- Кнопка → (если нужен ввод) **одно поле ввода** → **dry_run** → ✅ confirm → **краткий отчёт** + (опц.) notify_team.

### 0.3 Инварианты
- Никаких “магических” коммитов без preview.
- Никаких “двойных валют”/формул в тексте отчёта. Только то, что важно владельцу.
- Все ACTION шаблоны должны работать и без SIS (в DEMO) либо отдавать “SIS unavailable” корректно, не ломая бот.

---

## 1) Навигация (главное меню)

**Меню:**
1) 📊 **Отчёты**
2) ⚙️ **Системы**
3) 💸 **Цены**
4) 🧾 **Заказы**
5) 🏷️ **Скидки**
6) 📦 **Товары**
7) 👥 **Команда**
8) 🔧 **Advanced** (для тех, кто не боится кнопок)

> Внутри каждого раздела: 4–8 кнопок. Не делаем “простыню”.

---

## 2) Шаблоны REPORT (read-only)

### R-01: KPI Snapshot (снимок бизнеса)
- **Назначение:** “Дай картину за день/неделю”.
- **Триггеры (text/voice):** “kpi”, “сводка”, “как дела”, “дай цифры”.
- **Параметры:** период (`today|yesterday|7d|30d`).
- **Вывод (минимум):**
  - выручка
  - кол-во заказов
  - AOV (средний чек)
  - конверсия (если есть данные)
  - top-3 товара/лука
  - заказы “зависшие” (count)
- **Источник:** SIS gateway (если подключено) или DEMO.
- **Риск:** LOW.

### R-02: Orders Health (здоровье заказов)
- **Назначение:** понять где “горит”.
- **Триггеры:** “зависшие”, “проблемные заказы”, “заказы по статусам”.
- **Параметры:** период + фильтр статуса.
- **Вывод:** статусная матрица + список top-N проблемных.
- **Риск:** LOW.

### R-03: Revenue Trend (график)
- **Назначение:** тренд выручки и заказов.
- **Триггеры:** “график выручки”, “тренд”, “динамика”.
- **Параметры:** `7d|30d|90d`, агрегирование `day|week`.
- **Вывод:** PNG (и краткий текст).
- **Риск:** LOW.

### R-04: Weekly PDF Report (опционально)
- **Назначение:** отчёт “в один файл”.
- **Триггеры:** “weekly report”, “pdf отчёт”.
- **Параметры:** период.
- **Вывод:** PDF + ключевые выводы.
- **Риск:** LOW.

### R-05: Top Products / Looks
- **Назначение:** что реально продаёт.
- **Триггеры:** “топ товаров”, “что продаётся”.
- **Параметры:** период, top-N.
- **Вывод:** список + доля выручки.
- **Риск:** LOW.

### R-06: Coupons / Referrals (если есть)
- **Назначение:** эффективность промо.
- **Триггеры:** “купоны”, “рефералка”, “промо”.
- **Параметры:** период.
- **Вывод:** usage, выручка по купонам, топ кодов.
- **Риск:** LOW.

### R-07: Inventory / Stock Risk (если SIS хранит остатки)
- **Назначение:** что заканчивается.
- **Триггеры:** “остатки”, “заканчивается”, “stock”.
- **Параметры:** threshold N.
- **Вывод:** список товаров < N.
- **Риск:** LOW.

### R-08: System Preflight (проверка систем)
- **Назначение:** “взлёт самолёта”: все сервисы живы.
- **Триггеры:** “проверка систем”, “healthcheck”, “preflight”.
- **Вывод:**
  - OwnerBot DB/Redis ok
  - SIS gateway ok (latency + auth ok)
  - (future) SizeBot ok
  - (future) payments ok
- **Риск:** LOW.

### R-09: SIS Shadow Check (DEMO+SIS параллельно)
- **Назначение:** сверка: DEMO vs SIS, не ломая UX.
- **Триггеры:** “shadow check”, “сверка sis”.
- **Вывод:** “SIS ok / mismatch” + что именно не совпало.
- **Риск:** LOW.

---

## 3) Шаблоны ACTION (write)

### A-01: notify_team (сообщение команде)
- **Назначение:** быстро пнуть менеджеров.
- **Триггеры:** “уведомь команду”, “напиши менеджерам”, “notify”.
- **Параметры:** текст сообщения.
- **Preview:** кому уйдёт + текст.
- **Commit:** отправка в MANAGER_CHAT_IDS.
- **Риск:** LOW.

---

## 4) ACTION: Цены (самое важное)

### A-PR-01: Bump all prices by %
- **Назначение:** поднять цены на X% (кризис/курс/инфляция).
- **Триггеры:** “подними цены на 7%”, “увеличь цены”.
- **Параметры:** `percent` (0.1..200), rounding preset.
- **Preview:** affected_count + 5 примеров.
- **Commit:** SIS action API `/prices/bump/*`.
- **Риск:** HIGH.

### A-PR-02: Bump prices by amount (опционально)
- **Назначение:** добавить фиксированную надбавку на позицию.
- **Параметры:** `additive_amount` (в валюте витрины), rounding.
- **Риск:** HIGH.

### A-FX-01: FX Reprice (snapshot + markup + rounding)
- **Назначение:** пересчитать цены по курсу (без runtime конвертации).
- **Параметры:** rate_set, input_currency (advanced), markup%, markup_additive, rounding, anomaly threshold.
- **Preview:** max_delta%, warnings, examples.
- **Commit:** SIS `/actions/reprice/apply` (+force если нужно).
- **Риск:** HIGH.

### A-FX-02: Rollback last reprice
- **Назначение:** откатить последний репрайс (если “что-то пошло не так”).
- **Preview:** какой job откатывается.
- **Commit:** SIS rollback apply.
- **Риск:** HIGH.


### A-PR-04: SIS Templates UX (Owner console)
- **Навигация:** `Шаблоны → Цены`
- **Кнопки:**
  - `Поднять цены на %`
  - `FX пересчёт цен`
  - `Откат последнего FX`
- **Force UX:** если preview содержит `anomaly.over_threshold_count > 0` или warning `force required for apply`, показываются две кнопки:
  - `✅ Применить`
  - `⚠️ Применить несмотря на аномалию` (commit с `force=true`)
- **Dry-run отчёт (минимум):** `affected_count`, `max_delta_pct`, `anomaly.over_threshold_count`, `examples` (до 3).
- **Ошибки 409:**
  - anomaly confirm required → подсказка запустить заново и выбрать force
  - rollback without data → `Нет данных для отката`

### A-PR-03: Emergency “pause repricing” (future)
- **Назначение:** если рынок шатает, лучше заморозить репрайсы.
- **Примечание:** зависит от того, будет ли у SIS планировщик.
- **Риск:** MED.

---

## 5) ACTION: Товары (публикация/скрытие)

### A-PD-01: Hide products with stock < N
- **Назначение:** быстро скрыть то, чего почти нет.
- **Параметры:** threshold N.
- **Preview:** affected_count + примеры.
- **Commit:** SIS publish flags update.
- **Риск:** MED.

### A-PD-02: Unpublish category (future)
- **Назначение:** снять категорию с витрины.
- **Риск:** MED.

### A-PD-03: Hide “stale” products (future)
- **Назначение:** снять товары без продаж X дней.
- **Риск:** MED.

---

## 6) ACTION: Скидки (аккуратно)

### A-DC-01: Apply discount % to stock < N
- **Назначение:** распродать остатки.
- **Параметры:** threshold N, discount_percent.
- **Preview:** affected_count + 5 примеров.
- **Commit:** SIS update discount fields.
- **Риск:** MED.

### A-DC-02: Disable all discounts (future)
- **Назначение:** “обнулить промо” и пересобрать стратегию.
- **Риск:** MED/HIGH (зависит от объёма).

> Важно: скидки не пересчитываются “в деньги” при смене валют. Любые скидки — политика бизнеса.

---

## 7) ACTION: Заказы (операционка)

### A-OR-01: Flag order (у вас уже есть в OwnerBot DEMO)
- **Назначение:** пометить “зависшие”/“проблемные”/“ручная проверка”.
- **Параметры:** order_id, reason.
- **Риск:** MED.

### A-OR-02: Assign order to manager (future)
- **Назначение:** назначить ответственного.
- **Риск:** MED.

### A-OR-03: Bulk notify customers (future, осторожно)
- **Назначение:** массовые уведомления при форс-мажоре.
- **Риск:** HIGH (только с лимитами).

---

## 8) Advanced: Комбинированные сценарии (через голос/планер)

Эти сценарии должны быть покрыты голосом, но желательно иметь “подсказки” в UI:

### S-01: “Поднять цены на 7%, но скрыть товары с остатком < 3”
- План: bump_all → hide_by_stock
- Требует последовательных confirm.

### S-02: “Если аномалии > 25%, не применять, а уведомить”
- План: reprice preview → notify_team
- Без commit reprice.

### S-03: “Сделай скидку 30% на товары с остатком < 3 и уведомь команду”
- Plan: apply_discount_by_stock → notify_team

---

## 9) Voice mapping (подсказка планировщику)

### 9.1 Канонические intents
- `kpi_snapshot`
- `orders_health`
- `revenue_trend`
- `system_preflight`
- `sis_shadow_check`
- `notify_team`
- `prices_bump_all`
- `fx_reprice`
- `fx_rollback_last`
- `products_hide_by_stock`
- `discount_apply_by_stock`
- `order_flag`

### 9.2 Синонимы (RU)
- bump_all: “подними цены”, “увеличь цены”, “прибавь к ценам”, “накинь”
- reprice: “пересчитай по курсу”, “обнови курс”, “репрайс”
- rollback: “откат”, “верни назад”, “отмени пересчёт”
- hide_by_stock: “скрой что заканчивается”, “сними с витрины остатки”
- discount_by_stock: “распродажа остатков”, “скидка на остатки”
- preflight: “проверка систем”, “всё ли живо”, “коннект”
- shadow_check: “сверка”, “параллельная проверка”, “shadow”

---

## 10) MVP “Starter pack” (что включаем первым)

**REPORT:** R-01, R-02, R-03, R-08, R-09  
**ACTION:** A-01, A-PR-01, A-FX-01, A-FX-02, A-PD-01, A-DC-01

---

## 11) Notes for implementation

- Каждый шаблон должен иметь:
  - уникальный `template_id`,
  - схему параметров,
  - функцию `render_preview(preview_payload)`,
  - единый формат “summary + examples + warnings”.
- Все ACTION вызовы в SIS идут через SIS Action API (SIS-ACT-01) с correlation_id.
- В OwnerBot действия должны записываться в audit trail с payload_hash и статусами.



## 12) PR-OwnerBot-TPL-02: Templates → Товары / Луки / Скидки

### Товары
- **Опубликовать товары (по ID)** → `sis_products_publish` (`target_status=ACTIVE`).
- **Снять с публикации товары (по ID)** → `sis_products_publish` (`target_status=ARCHIVED`).
- **Опубликовать ВСЕ товары** → `status_from=ARCHIVED`, `target_status=ACTIVE`.
- **Снять с публикации ВСЕ товары** → `status_from=ACTIVE`, `target_status=ARCHIVED`.

### Луки
- **Опубликовать луки (по ID)** → `sis_looks_publish` (`target_active=true`).
- **Снять с публикации луки (по ID)** → `sis_looks_publish` (`target_active=false`).
- **Опубликовать ВСЕ луки / Снять ВСЕ луки** через `is_active_from`.

### Скидки
- **Удалить скидки (по ID товаров)** → `sis_discounts_clear`.
- **Удалить ВСЕ скидки** → `sis_discounts_clear` без `product_ids`.
- **Поставить скидку % (по ID товаров)** → `sis_discounts_set` (ввод IDs + `%`).
- **Поставить скидку % на остатки <= N** → `sis_discounts_set` (ввод `N` + `%`).

### Ввод и валидация (OwnerBot side)
- IDs: разделители `,` / пробел / новая строка; максимум 200 ID.
- `%`: целое 1..95.
- `N`: целое 1..9999.

### Safety / FORCE
- Dry-run всегда перед commit.
- FORCE-кнопка показывается, если есть warning code `FORCE_REQUIRED` или `MASS_CHANGE_OVER*`.
- Для обратной совместимости также учитывается legacy warning text `force required for apply`.
- FORCE commit отправляет `payload_commit.force=true`.
