# OwnerBot DEMO Mode Analysis

## Текущее состояние

### Что есть сейчас

**DEMO режим уже полностью реализован!**

При запуске OwnerBot в режиме `UPSTREAM_MODE=DEMO`:

1. **Собственная БД OwnerBot** (`ownerbot_db` в Docker)
2. **Автоматический seed** данных при старте бота
3. **Таблицы с демо-данными** для всех ключевых сценариев

### Демо-таблицы (PostgreSQL в `ownerbot_db`)

| Таблица | Назначение | Сид данные |
|---------|------------|------------|
| `ownerbot_demo_kpi_daily` | KPI по дням | 14 дней истории |
| `ownerbot_demo_orders` | Заказы | 70 заказов (10 активных + 60 исторических) |
| `ownerbot_demo_order_items` | Позиции заказов | Связанные с заказами |
| `ownerbot_demo_products` | Товары | 24 товара (5 категорий) |
| `ownerbot_demo_coupons` | Купоны | 4 купона |
| `ownerbot_demo_chat_threads` | Чаты поддержки | 6 тредов |

### Демо товары (24 шт.)

```
Тайтсы: 4 товара (Core Black, Motion Grey, Zero Stock, Draft)
Топы: 4 товара (Breeze White, Breeze Mint, Active Noir, Archive)
Худи: 4 товара (Urban Sand, Urban Coal, Lite, Proto)
Аксессуары: 4 товара (Bottle, Bag, Belt, Clip)
Верхняя одежда: 4 товара (Storm, Breeze, Lab, Urban)
Базовые: 4 товара (Носки Core, Flex, Sample, Hidden)
```

Товары имеют разные состояния:
- С ценой / без цены (price=0)
- Опубликованные / неопубликованные
- С фото / без фото / без видео
- С остатком / нет в наличии
- С флагом возврата / без

### Демо заказы (70 шт.)

**Активные (10 шт.):**
- OB-1001: pending (ожидает оплаты)
- OB-1002: paid, shipping pending (оплачен, ждёт отгрузки)
- OB-1003: stuck, payment failed
- OB-1004: paid, shipped
- OB-1005: stuck, payment pending
- OB-1006: pending
- OB-1007: paid, late ship (просрочена отгрузка)
- OB-1008: stuck, payment failed
- OB-1009: paid, shipping pending (купон WELCOME10)
- OB-1010: paid, refunded

**Исторические (60 шт.):**
- OB-2001...OB-2060: оплаченные заказы за последние 45 дней

### KPI данные (14 дней)

```
revenue_gross: 900...1400 EUR
revenue_net: 820...1200 EUR  
orders_paid: 8-14 в день
orders_created: 12-17 в день
aov: 75-104 EUR
```

### Купоны (4 шт.)

| Код | Скидка | Статус | Использований |
|-----|--------|--------|---------------|
| WELCOME10 | 10% | Активен | 34/200 |
| VIP25 | 25% | Активен | 22/80 |
| SHIP5 | -5€ | Активен | 10/120 |
| SPRING15 | 15% | Истёк | 100/100 |

### Чаты поддержки (6 тредов)

- TH-2001: открыт, без ответа 2ч
- TH-2002: открыт, есть ответ
- TH-2003: открыт, без ответа 12ч
- TH-2004: закрыт
- TH-2005: открыт, без ответа 9ч
- TH-2006: открыт, есть ответ

---

## Какие tools работают в DEMO

### ✅ Полностью работают на демо-данных

| Tool | Источник данных | Статус |
|------|-----------------|--------|
| `kpi_snapshot` | `ownerbot_demo_kpi_daily` | ✅ Работает |
| `revenue_trend` | `ownerbot_demo_kpi_daily` | ✅ Работает |
| `orders_search` | `ownerbot_demo_orders` | ✅ Работает |
| `top_products` | `ownerbot_demo_order_items` + products | ✅ Работает |
| `inventory_status` | `ownerbot_demo_products` | ✅ Работает |
| `chats_unanswered` | `ownerbot_demo_chat_threads` | ✅ Работает |
| `coupons_status` | `ownerbot_demo_coupons` | ✅ Работает |
| `coupons_top_used` | `ownerbot_demo_coupons` | ✅ Работает |
| `flag_order` | `ownerbot_demo_orders` | ✅ Работает |
| `kpi_compare` | `ownerbot_demo_kpi_daily` | ✅ Работает |

### ⚠️ Stub / требуют SIS

| Tool | Статус | Причина |
|------|--------|---------|
| `sis_fx_reprice` | ❌ Требует SIS | FX API только в SIS |
| `sis_prices_bump` | ❌ Требует SIS | Цены только в SIS |
| `sis_products_publish` | ❌ Требует SIS | Публикация в SIS |
| `sis_looks_publish` | ❌ Требует SIS | Луки в SIS |
| `sis_discounts_set` | ❌ Требует SIS | Скидки в SIS |
| `funnel_snapshot` | ⚠️ Stub | Не реализован |
| `refunds_anomalies` | ⚠️ Stub | Не реализован |
| `truststack_signals` | ⚠️ Stub | Не реализован |
| `pause_campaign` | ⚠️ Stub | Не реализован |

---

## Как проверить работу DEMO

### Команды для тестирования

```bash
# В Telegram боте:

/start                    # Статус системы
/templates                # Меню шаблонов
/systems                  # Диагностика
/tools                    # Список инструментов

# Прямые команды:
/help                     # Примеры фраз
```

### Шаблоны для тестирования

**📊 Отчёты:**
- KPI сегодня/вчера/7д/30д
- Тренд выручки (PNG)
- Weekly PDF

**🧾 Заказы:**
- Зависшие заказы
- Поздняя отгрузка
- Проблемы оплаты
- Поиск по ID/телефону

**📦 Товары:**
- Инвентарь
- Топ продаж
- Без фото/цены

**🔔 Уведомления:**
- Статус подписок
- Дайджест

---

## Что НЕ работает в DEMO (и почему)

### FX/Pricing tools

```python
# sis_fx_reprice.py
if settings.upstream_mode == "DEMO":
    return ToolResponse.fail(
        code="DEMO_NOT_SUPPORTED",
        message="FX reprice is not available in DEMO mode..."
    )
```

**Причина:** FX пересчёт требует реальных курсов валют и цен из SIS.

### SIS Actions tools

Все `sis_*` tools требуют подключения к SIS API:
- `sis_products_publish`
- `sis_looks_publish`  
- `sis_discounts_set`
- `sis_discounts_clear`

---

## Предложение: Расширенный демо-режим

### Вариант A: Текущий режим достаточен

**Что уже можно тестировать:**
- ✅ Аналитика и KPI
- ✅ Поиск заказов
- ✅ Топ продуктов
- ✅ Инвентарь
- ✅ Уведомления и дайджесты
- ✅ Чаты поддержки
- ✅ Купоны

**Достаточно для:**
- Проверки UI
- Тестирования шаблонов
- Демонстрации функционала
- Разработки новых features

### Вариант B: Отдельный SIS Demo Server

**Когда нужен:**
- Тестирование FX пересчёта
- Тестирование публикации товаров/луков
- Полный E2E flow с SIS

**Реализация:**
```yaml
# docker-compose.demo.yml
services:
  sis_demo:
    image: sis:latest
    environment:
      - DATABASE_URL=postgresql://sis_demo:...
      - DEMO_MODE=true
    ports:
      - "8001:8000"
  
  sis_demo_db:
    image: postgres:16
    environment:
      - POSTGRES_DB=sis_demo
```

**Затраты:**
- Поднять SIS контейнер
- Настроить baseline с демо-данными
- Связать OwnerBot → SIS Demo

---

## Рекомендация

### Для текущего этапа: Вариант A

**Достаточно текущих демо-данных для:**
1. Разработки нового UI
2. Тестирования 80% функционала
3. Демонстрации клиенту

**Действия:**
1. Проверить что `seed_demo_data()` вызывается при старте
2. Протестировать шаблоны через `/templates`
3. Проверить генерацию отчётов (KPI, тренды)

### На будущее: Вариант B

**Когда:**
- Нужно тестировать FX
- Нужен E2E flow с SIS
- Staging среда для QA

---

## Как проверить текущие демо-данные

```sql
-- Подключиться к ownerbot_db
docker compose exec ownerbot_db psql -U ownerbot -d ownerbot

-- Проверить KPI
SELECT * FROM ownerbot_demo_kpi_daily ORDER BY day DESC LIMIT 5;

-- Проверить заказы
SELECT order_id, status, amount, payment_status 
FROM ownerbot_demo_orders 
WHERE status IN ('stuck', 'pending') 
LIMIT 10;

-- Проверить товары
SELECT product_id, title, price, stock_qty, published 
FROM ownerbot_demo_products 
WHERE stock_qty = 0 OR price = 0;

-- Проверить купоны
SELECT code, percent_off, used_count, max_uses, active 
FROM ownerbot_demo_coupons;
```

---

## Резюме

| Вопрос | Ответ |
|--------|-------|
| Есть ли демо-данные? | ✅ Да, полный набор |
| Работает ли аналитика? | ✅ Да |
| Работают ли шаблоны? | ✅ Да (80%) |
| Нужен ли отдельный сервер? | ❌ Нет (пока) |
| Можно ли тестировать UI? | ✅ Да |
| Можно ли тестировать LLM? | ✅ Да (фразы → tools) |

**Вывод:** Текущий DEMO режим достаточен для разработки и тестирования UI. Отдельный SIS Demo Server нужен только для E2E тестирования FX и SIS Actions.
