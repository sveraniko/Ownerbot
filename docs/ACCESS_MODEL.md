# ACCESS_MODEL.md
> Модель доступа SIS (Gate + Levels + Content + Renew).  
> Цель: одна понятная система, без “в одном месте разрешили, в другом забыли”.

---

## 0) TL;DR
- У нас **двухуровневый** доступ:
  1) **Global Gate**: “магазин закрыт/открыт” (AccessControlService.check_access)
  2) **Target Access (Levels)**: доступ к конкретным целям (product/look) по действиям (VIEW/BUY/CONTENT)

- **TargetGuard** – единая точка отображения locked‑экрана (CTA: вступить в клуб / купить ключ / купить доступ / продлить).

- “Контент” – это не “описание карточки”. Контент = **дополнительные блоки/файлы/аудио/материалы**, которые открываются только при CONTENT‑доступе.

- **Renew** добавляет смысл “доступ был, но истёк” и формирует отдельный CTA (Продлить).

---

## 1) Сущности и уровни
### 1.1 Global Gate
Сущность: `Business.access_control_enabled` + grants/keys/blocks

Результат: `AccessDecision`:
- ok_open
- ok_granted
- denied_* (blocked, no_key, etc.)

Global Gate применяется:
- при входе в каталог / старте
- на уровне “пускать/не пускать вообще”.

### 1.2 Target Access Policy (Levels)
Сущность: `AccessPolicy` (на target_type + target_id)

Policy включает:
- active/inactive
- lock flags на действия: view/buy/content
- unlock_mode:
  - public (fail-open)
  - club
  - entitlement
  - key_offer
  - (плюс “buy” semantics, если buy gated отдельно)

Результат: `TargetAccessDecision`:
- allowed: ok_public / ok_granted / ok_owner/admin (если есть)
- denied: reason_code + unlock CTA hints
- expired: deny_reason=expired + renew_supported + expired_at

### 1.3 Entitlement / Grant / Key Offer
- Entitlement: право доступа, может иметь TTL
- Grant: выдача права пользователю
- Key / KeyOffer: ключи/офферы для разблокировки

---

## 2) Target Actions
Действия определяют **какой кусок UX** мы защищаем:
- `VIEW`: возможность увидеть/открыть карточку/страницу
- `BUY`: возможность купить (добавить в корзину/checkout)
- `CONTENT`: возможность увидеть контентные блоки (материалы)

### 2.1 Принцип
- VIEW защищает “вход”
- BUY защищает “монетизацию”
- CONTENT защищает “доставку ценности”

---

## 3) SSOT: кто решает доступ
### 3.1 Canonical source
Решение по доступу = `TargetAccessService.check_target_access(...)`

Только он имеет право:
- интерпретировать policy
- учитывать entitlement grants и TTL
- формировать deny_reason/expired_at
- выдавать deny‑context для UI

### 3.2 TargetGuard
TargetGuard:
- принимает TargetAccessDecision
- рендерит locked UI одинаково для всех мест
- не должен “сам решать” доступ, только отображать

---

## 4) Где проверяется доступ
### 4.1 Каталог / карточки
- list pages: можно показывать статусы (🆓✅🔒⌛) батчем, без чеков на каждый item
- open card: VIEW check
- buy: BUY check
- “подробнее/контент”: CONTENT check

### 4.2 Orders → Content
- Доступ к контенту из заказа:
  - проверить ownership заказа
  - проверить что продукт принадлежит заказу
  - вызвать TargetGuard(CONTENT) на этот продукт

---

## 5) Expiry / Renew semantics
### 5.1 Что такое expiry
Expiry – ситуация:
- когда **раньше** entitlement был,
- но теперь истёк,
- и мы хотим показать “доступ истёк” вместо “нет доступа”.

### 5.2 Зачем это важно
- UX: “тебя не отвергли”, просто срок закончился
- Монетизация: кнопка “Продлить” вместо “Купить заново” или “Ввести ключ”

### 5.3 Renew supported
Решение о том, поддерживается ли renew:
- часть policy/entitlement mode
- в TargetAccessDecision `renew_supported=True/False`

---

## 6) UI политики для доступа (без ломания интерфейса)
### 6.1 Не меняем базовую карточку
Карточка товара/лука остаётся прежней:
- “Ещё” – расширенная карточка
- “Подробнее” – контентные блоки

Мы не вводим “третью сущность” UI.

### 6.2 Locked UI
Если действие запрещено:
- вместо контента: locked screen
- вместо buy: locked buy CTA
- вместо open: locked screen

### 6.3 Course Mode
Course Mode для look = косметика + доп. UX:
- композиция = “Блоки курса”
- статусы блоков = 🆓✅🔒⌛
- быстрые CTA: купить/продлить

Это не меняет access модель, только интерфейс вокруг composition.

---

## 7) Fail-open vs Fail-closed
### 7.1 Default public
Если policy отсутствует:
- доступ считается public (fail-open)
- важно отображать это в админке (“Policy: отсутствует (public)”)

### 7.2 Когда fail-closed
Если policy есть и active:
- запрещаем lock'ed action
- выдаём deny decision с CTA

---

## 8) Админ права
- Создание/изменение policy – owner‑only (не generic admin)
- Это защита от “поигрались в настройки и убили выручку”.

---

## 9) Аналитика
### 9.1 Что логируем
- TARGET_ACCESS_GRANTED / DENIED
- reason_code
- unlock_mode
- action
- target

### 9.2 Для чего Owner‑Bot
- видеть где “узкие места”
- видеть что чаще всего блокируется
- прогнозировать revenue leaks

---

## 10) Набор инвариантов (must not break)
- Global Gate остаётся отдельным механизмом (не смешивать с Levels)
- check_target_access не меняет semantics без нужды
- locked UI рендерится только TargetGuard
- отсутствие policy = public (fail-open) и это видно в админке
- TTL только у entitlements/keys (а не “у карточки” напрямую)

---

## 11) Чеклист для PR, который трогает access
- [ ] Есть unit tests на: no policy, active grant, denied, expired
- [ ] Нет дублирования логики access outside TargetAccessService
- [ ] TargetGuard UI остаётся унифицированным
- [ ] Baseline updated, если добавили таблицы/поля
- [ ] Не сломаны existing flows: catalog, checkout, orders

---

## 12) Маппинг сценариев (примеры)
### 12.1 Инфокурс
- Look = курс (course mode)
- Product = блок курса
- VIEW: можно видеть карточку (по желанию)
- BUY: блокируется до оплаты/доступа
- CONTENT: открывается после покупки/entitlement
- TTL: ограничение доступа по времени (renew)

### 12.2 VIP клуб (Rolex‑style)
- Level/Policy: требует club/entitlement
- BUY/CONTENT блокируется
- CTA: “Вступить/получить ключ/купить доступ”
- Renew: по истечении подписки

### 12.3 Adult
- По сути то же, что клуб, только другие названия и коммуникация.
- Модель доступа та же.

### 12.4 Услуги (пока без полноценного booking)
- BUY может быть gated “только по коду/после консультации”
- CONTENT может включать инструкции/условия

---

## 13) Вопросы на будущее (не сейчас)
- API/Read model для Owner‑Bot: access denies by target
- Авто‑renew через staged payments
- “bundled entitlements” (купил look → открыл N продуктов)

